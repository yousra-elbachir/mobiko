# ============================================================================
# This repo publishes ONLY these project folders (everything else stays private):
#   - annotation_app/         (Streamlit tool for Mark & Davnah)
#   - annotation_triplet/     (shared with mobiko coworkers)
#   - annotation_relation/    (shared with mobiko coworkers)
# ============================================================================
/*
!/.gitignore
!/annotation_app/
!/annotation_triplet/
!/annotation_relation/

# Defense-in-depth: never publish secrets or venvs from ANY included folder.
# (data/ and outputs/ ARE published for the extraction folders, on request.)
**/tokens/
**/__pycache__/
**/.venv/
**/.ipynb_checkpoints/
*.rtf
*.pem
*.key
.env
.env.*
.DS_Store

# Excluded on request (may contain notebook cell outputs)
annotation_relation/relation_extraction.ipynb

# Run artifacts (per-paper "done" markers)
*.done
.*.done
